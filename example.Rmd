Test using markdown and imguR
-------------

```{r}
library(knitr)
opts_knit$set(upload.fun = imgur_upload, base.url = NULL) # upload all images to imgur.com
opts_chunk$set(fig.width=3.5, fig.height=3.5, cache=TRUE)
## a simple calculator
1+1
## boring random numbers
set.seed(123)
rnorm(5)
par(mar = c(4, 4, .1, .1))
plot(mpg~hp, data=mtcars, pch=19)
par(mar = c(3, 2, .1, .1))
matplot(t(scale(mtcars)), lty=1, type='l', xaxt='n', ylab='')
axis(1, seq(ncol(mtcars)), colnames(mtcars))
```
